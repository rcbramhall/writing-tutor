GROQ_API_KEY=your-groq-api-key-here
PORT=3000

# Optional locally, REQUIRED before deploying publicly.
# Gates the whole app behind HTTP Basic Auth (any username, this password).
# Leave unset for open access during local development.
APP_PASSWORD=choose-a-shared-password
